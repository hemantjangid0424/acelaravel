import { defineNuxtPlugin, useRuntimeConfig } from "#imports";
import { VueRecaptchaPlugin } from "./index.mjs";
export default defineNuxtPlugin(({ vueApp }) => {
  const {
    public: { recaptcha }
  } = useRuntimeConfig();
  vueApp.use(VueRecaptchaPlugin, recaptcha);
});
